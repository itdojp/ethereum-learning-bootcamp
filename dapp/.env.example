# Copy to `.env.local` and fill in values.
# NOTE: The current implementation uses the injected wallet provider (MetaMask, etc.).

VITE_CHAIN_ID=31337
VITE_TOKEN_ADDRESS=
VITE_EVENT_TOKEN=

# Optional (reserved for future use, e.g. adding a chain)
VITE_RPC=http://127.0.0.1:8545
